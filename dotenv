# Make a copy of this file into .env and add your variables

# generate the following codes with the command `rake secret`
APP_SECRET:
DEVISE_SECRET:

# Create a bucket on Amazon S3 (object storage service) to store companies logo
AWS_KEY_ID:
AWS_SECRET_KEY:
AWS_REGION:
AWS_BUCKET:

# get a free sendgrid account at https://sendgrid.com/
SENDGRID_USERNAME:
SENDGRID_PASSWORD:

